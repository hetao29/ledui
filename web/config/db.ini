default{

        main{
                host localhost;
                user root;
                database ledui;
                password postcard;
                charset utf8;
        }
}
